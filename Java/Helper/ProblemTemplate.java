package Helper;

/**
 * Question statement
 */
public class ProblemTemplate {

    // T = O(n) | S = O(n)
    // Where n is input array's length.
    public static int solve(int[] array) {
        return 0;
    }
}
